server:
  port: 80
system-config:
  txtPath: "E://test/txt"
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/txt?useUnicode=true&amp;characterEncoding=UTF-8
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  file: E://test/nix_book/spring-boot-logging.log
  level:
      com.dragon.study.spring.boot: DEBUG